- Researched Snowflake cost attribution primitives for FinOps Native App: object tags + query tags; query-level attribution via ACCOUNT_USAGE.QUERY_ATTRIBUTION_HISTORY (idle excluded, <=~100ms excluded, up to ~8h latency).
- Drafted SQL to allocate warehouse idle time proportionally to QUERY_TAG by reconciling WAREHOUSE_METERING_HISTORY vs QUERY_ATTRIBUTION_HISTORY; noted no ORG_USAGE equivalent for query-level attribution.
- Noted compute billing nuance: cloud services credits are billed only if daily consumption exceeds 10% of daily warehouse usage; Snowflake points to METERING_DAILY_HISTORY for billed-cloud-services determination.
- Identified APPLICATION_DAILY_USAGE_HISTORY as a native data source for tracking Snowflake Native App credit usage (last 365 days) within an account.
- Confirmed ACCOUNT_USAGE.TAG_REFERENCES is direct-only (no inheritance) and includes APPLY_METHOD + ~2h latency; good for building a tag mapping dimension.
- Captured resource monitor constraints: warehouse-only, can notify/suspend; not for serverless/AI (use budgets).
- Drafted a warehouse burn-rate + month-end forecasting SQL pattern using ACCOUNT_USAGE.WAREHOUSE_METERING_HISTORY to drive “days-to-threshold” UX for resource monitors.
- Wrote a durable research note + concrete SQL artifact for allocating billed warehouse credits to QUERY_TAG (including idle allocation) as the foundation for a FinOps semantic layer.
- Captured Snowflake’s documented limitation that there is **no ORG_USAGE equivalent** for query-level attribution (must roll up per account).
- Added org-level billing primitives for the app: ORG_USAGE.USAGE_IN_CURRENCY_DAILY (currency + billing metadata, up to 72h latency, can change until month close) and ORG_USAGE.WAREHOUSE_METERING_HISTORY (hourly credits across all accounts). Drafted a heuristic SQL allocation from org-day warehouse $ → warehouses.
- New finops note (2026-03-04): consolidated core cost-intelligence sources (ORG_USAGE.USAGE_IN_CURRENCY_DAILY, ACCOUNT_USAGE.QUERY_HISTORY, ACCOUNT_USAGE.WAREHOUSE_METERING_HISTORY) + drafted a v0 SQL allocation model from hourly warehouse credits → query-level credits (by overlap).