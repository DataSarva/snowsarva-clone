- Mission Control research (finops): documented Snowflake’s recommended cost attribution approach (object tags + query tags), key usage views (ACCOUNT_USAGE / ORG_USAGE), and a concrete SQL draft to reconcile per-query attribution to warehouse metering by allocating idle time proportionally.
- Noted key caveats: QUERY_ATTRIBUTION_HISTORY excludes idle time + very short queries (~<=100ms) and can lag up to ~8 hours; cloud services billing follows the 10% daily threshold and should use METERING_DAILY_HISTORY for billed credits.
- New findings: Budgets support notifications via email/cloud queues/webhooks + user-defined stored-procedure actions; low-latency budgets can refresh hourly but cost ~12x more than default refresh tier. Resource monitors only cover warehouses (not serverless/AI), so “budgets vs resource monitors” is a key product decision point.
