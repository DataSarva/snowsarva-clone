- Snowflake updates watch: Mar 1/2 2026 changes — hybrid tables pricing simplified (no request/serverless credit line item), budgets can trigger stored procs (threshold + cycle-start), Native Apps configuration (Preview, supports sensitive config), backups now allow unlimited backup sets per object.
