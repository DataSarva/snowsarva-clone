- Ran FinOps research on Snowflake cost attribution primitives (tags + query attribution + idle allocation). Produced research note + SQL drafts for warehouse tag showback + service-type credit breakdown.
- Key takeaway: org-wide cost attribution works well for *dedicated* resources via ORGANIZATION_USAGE (plus TAG_REFERENCES in org account), but query-level attribution remains account-scoped via QUERY_ATTRIBUTION_HISTORY.
